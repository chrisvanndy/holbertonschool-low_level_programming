#include "holberton.h"
/**
 * _islower - returns 1 if lowercase integer
 * otherwise returns 0
 *
 * Return: 0
 */
int _islower(int c)
{

	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	else

	return (0);
}
