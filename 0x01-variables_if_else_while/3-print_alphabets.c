#include <stdio.h>
/**
 * Main - this is my function
 * int c,v - lowercase and capital variables
 *
 * Return: 0
 */
int main(void)
{
	char c, v;
{
	for (c = 'a'; c <= 'z'; c++)
	putchar (c);
}
{
	for (v = 'A'; v <= 'Z'; v++)
	putchar (v);
	putchar ('\n');
}
	return (0);
}
